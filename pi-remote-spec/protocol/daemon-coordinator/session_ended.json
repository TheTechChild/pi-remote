{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/TheTechChild/pi-remote-spec/protocol/daemon-coordinator/session_ended.json",
  "title": "session_ended",
  "description": "Daemon -> Coordinator. Pi process ended. See SPEC.md §§ 10.2, 10.3.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "v", "session_id", "seq", "reason"],
  "properties": {
    "type": { "const": "session_ended" },
    "v": { "const": 1 },
    "session_id": { "type": "string" },
    "seq": { "type": "integer", "minimum": 1 },
    "reason": {
      "type": "string",
      "enum": [
        "process_exit",
        "extension_disconnect",
        "tmux_server_lost",
        "killed",
        "spawn_failed"
      ]
    }
  }
}
