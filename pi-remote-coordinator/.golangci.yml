# SPDX-License-Identifier: MIT
version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
  exclusions:
    paths:
      - internal/proto
      - .codegen-cache

formatters:
  enable:
    - gofmt
