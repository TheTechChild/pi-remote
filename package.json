{
  "name": "@thetechchild/pi-remote",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pi Remote — session-sharing for the Pi coding agent. This package.json is the pi-package entry point and the monorepo workspace orchestrator. The extension's dependencies live in pi-remote-ext/package.json; the daemon, coordinator, and Android app live in their own subdirectories with their own toolchains.",
  "license": "MIT",
  "author": "Clayton",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheTechChild/pi-remote.git"
  },
  "keywords": ["pi-package"],
  "workspaces": ["pi-remote-ext"],
  "scripts": {
    "build": "yarn workspace pi-remote-ext build",
    "typecheck": "yarn workspace pi-remote-ext typecheck",
    "lint": "yarn workspace pi-remote-ext lint",
    "format": "yarn workspace pi-remote-ext format",
    "test": "yarn workspace pi-remote-ext test",
    "test:watch": "yarn workspace pi-remote-ext test:watch",
    "codegen": "yarn workspace pi-remote-ext codegen"
  },
  "engines": {
    "node": ">=22"
  },
  "pi": {
    "extensions": ["./pi-remote-ext/src/index.ts"]
  }
}
